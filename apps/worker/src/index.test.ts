import assert from "node:assert/strict";
import { describe, it } from "node:test";

describe("@reviewpulse/worker", () => {
  it("is a WP0 stub (no sync yet)", () => {
    assert.equal("WP0", "WP0");
  });
});
